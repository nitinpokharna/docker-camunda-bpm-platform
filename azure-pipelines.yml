# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- next

pool:
  vmImage: ubuntu-latest

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'
- task: Docker@2
  inputs:
    containerRegistry: 'nitinacr'
    repository: 'nitinpacr'
    command: 'build'
    Dockerfile: '**/Dockerfile'
    arguments: '--build-arg EE=true --build-arg DISTRO=${DISTRO} --build-arg VERSION=7.8.1 --build-arg USER=trial_nitin_pokharna_94_gmail_com  --build-arg PASSWORD=b99295bd-9dc5-482a-9'
- task: Docker@2
  inputs:
    containerRegistry: 'nitinacr'
    repository: 'nitinpacr'
    command: 'push'
    tags: 'Camundabpm'
- script: |
    echo Add other tasks to build, test, and deploy your project.
    echo See https://aka.ms/yaml
  displayName: 'Run a multi-line script'
